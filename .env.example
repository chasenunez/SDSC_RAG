# Copy to .env and fill in. Only the final answer step needs this key;
# data setup, the graph, and retrieval all run without it.
OPENROUTER_API_KEY=

# Optional. Any OpenRouter model id. Default is set in src/geokg/config.py.
OPENROUTER_MODEL=
